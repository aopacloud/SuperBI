# Tomcat
server:
  port: 10991

# Spring
spring: 
  application:
    # 应用名称
    name: bdp-super-bi
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 192.168.11.205:8848
#        username: nacos
#        password: nacos
      config:
        # 配置中心地址
        server-addr: 192.168.11.205:8848
#        username: nacos
#        password: nacos
        # 配置文件格式
        file-extension: yml
#        namespace: a1723e2d-b3a9-495c-973e-aec323327822
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
